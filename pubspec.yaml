name: os_release
description: A library to help get Linux os-release information.
version: 1.0.0
repository: https://github.com/alastairlundy/os_release

platforms:
  linux:

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:

dev_dependencies:
  checks: ^0.2.1
  lints: ^3.0.0
  test: ^1.24.0
